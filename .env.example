POSTGRES_DB=nauticalmeal
POSTGRES_USER=nauticalmeal
POSTGRES_PASSWORD=changeme
POSTGRES_HOST=localhost
POSTGRES_PORT=5432

# Used when the backend runs on the host (not in Docker)
OPENAI_API_KEY=

# docker compose sets AI_PROVIDER=ollama and points at the ollama service.
# Override here only if you want a different default.
# AI_PROVIDER=openai
# OLLAMA_MODEL=qwen2.5:7b-instruct
